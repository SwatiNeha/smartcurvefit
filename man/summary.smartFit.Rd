% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_model.R
\name{summary.smartFit}
\alias{summary.smartFit}
\title{Extended Summary of a SmartCurveFit Model}
\usage{
\method{summary}{smartFit}(object, ...)
}
\arguments{
\item{object}{An object of class \code{"smartFit"}.}

\item{...}{Arguments passed for compatibility with the generic
\code{\link[base:summary]{base::summary()}}. They are ignored by \code{summary.smartFit()}; supplying
additional arguments will trigger a warning.}
}
\value{
Invisibly returns the input \code{"smartFit"} object.
}
\description{
Produces a detailed report of a fitted \code{"smartFit"} object, including:
\itemize{
\item model type, chosen loss, and selection metric
\item estimated coefficients
\item fit metrics (RMSE, MAE, MSE)
\item residual distribution summary
\item full cross-validation results
\item full-data refits under all candidate losses
}
}
\details{
Unlike \code{print.smartFit()}, this method is verbose and intended for
model diagnostics. It is especially useful for checking residual
behaviour, loss function comparisons, and coefficient stability.

Any extra arguments passed via \code{...} are ignored with a warning.
}
\examples{
set.seed(123)
x <- 1:30; y <- 3 * exp(0.2 * x) + rnorm(30)
fit <- fit_model(x, y, "exponential")
summary(fit)
summary(fit, extra = TRUE) # triggers unused-argument warning

}
