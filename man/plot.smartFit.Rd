% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_model.R
\name{plot.smartFit}
\alias{plot.smartFit}
\title{Plot a SmartCurveFit Model}
\usage{
\method{plot}{smartFit}(
  x,
  y,
  ...,
  show_residuals = FALSE,
  col_points = "steelblue",
  pch_points = 19,
  col_line = "red",
  lwd_line = 2
)
}
\arguments{
\item{x}{An object of class \code{"smartFit"}.}

\item{y}{Ignored; included for consistency with the generic \code{plot()}.}

\item{...}{Additional graphical arguments passed to \code{\link[graphics:plot.default]{graphics::plot()}}.
Invalid arguments are ignored with a warning. Included for compatibility
with the base \code{plot()} generic.}

\item{show_residuals}{Logical; if \code{TRUE}, draws residuals vs fitted values
in a second panel.}

\item{col_points}{Color for observed data points (default \code{"steelblue"}).}

\item{pch_points}{Plot symbol for observed data (default \code{19}).}

\item{col_line}{Color for fitted curve (default \code{"red"}).}

\item{lwd_line}{Line width for fitted curve (default \code{2}).}
}
\value{
Invisibly returns the input \code{"smartFit"} object.
}
\description{
Visualises observed data points and the fitted curve from a \code{"smartFit"}
object. Optionally, a residuals plot can also be drawn.
}
\details{
\itemize{
\item By default, the upper plot shows the observed \verb{(x, y)} values and the
fitted curve under the chosen model/loss.
\item If \code{show_residuals = TRUE}, an additional panel displays residuals
versus fitted values.
\item Graphical arguments like \code{col} and \code{pch} are mapped to control point
appearance. Invalid arguments are ignored with a warning.
}
}
\examples{
set.seed(321)
x <- 1:50; y <- 3 + 2 * log(x) + rnorm(50)
fit <- fit_model(x, y, "logarithmic")

# Default plot
plot(fit)

# With residuals
plot(fit, show_residuals = TRUE)

# Custom styling
plot(fit, col_points = "darkgreen", col_line = "orange", lwd_line = 3)

# Invalid argument triggers warning
plot(fit, badarg = 99)

}
