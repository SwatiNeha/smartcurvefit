% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_model.R
\name{predict.smartFit}
\alias{predict.smartFit}
\title{Predict from a SmartCurveFit Model}
\usage{
\method{predict}{smartFit}(object, newdata = NULL, ...)
}
\arguments{
\item{object}{An object of class \code{"smartFit"}, returned by \code{\link[=fit_model]{fit_model()}}.}

\item{newdata}{Numeric vector of predictor values at which to compute predictions.
If omitted, predictions are returned for the training data used to fit the model.}

\item{...}{Arguments passed for compatibility with the generic \code{\link[stats:predict]{stats::predict()}}.
Extra arguments are ignored with a warning.}
}
\value{
A numeric vector of predicted values corresponding to \code{newdata} (or training data if \code{newdata} is missing).
}
\description{
Generates predictions from a fitted \code{"smartFit"} object for new values of \code{x}.
}
\details{
The prediction formula depends on the model type:
\itemize{
\item \strong{Power law:}     \eqn{y = a * x^b}
\item \strong{Exponential:}   \eqn{y = a * exp(b * x)}
\item \strong{Logarithmic:}   \eqn{y = a + b * log(x)}
}
}
\examples{
set.seed(101)
x <- 1:20
y <- 2 * x^1.2 + rnorm(20)
fit <- fit_model(x, y, model_type = "power_law")

# Predict on training data
preds_train <- predict(fit)
head(preds_train)

# Predict on new data
new_x <- seq(1, 25, by = 0.5)
preds_new <- predict(fit, newdata = new_x)
head(preds_new)

}
