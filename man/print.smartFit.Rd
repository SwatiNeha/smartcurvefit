% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_model.R
\name{print.smartFit}
\alias{print.smartFit}
\title{Print a SmartCurveFit Model}
\usage{
\method{print}{smartFit}(x, ...)
}
\arguments{
\item{x}{An object of class \code{"smartFit"}.}

\item{...}{Arguments passed for compatibility with the generic
\code{\link[base:print]{base::print()}}. They are ignored by \code{print.smartFit()}; supplying
additional arguments will trigger a warning.}
}
\value{
Invisibly returns the input \code{"smartFit"} object.
}
\description{
Provides a concise textual summary of a fitted \code{"smartFit"} object.
The output includes:
\itemize{
\item model type
\item chosen loss and selection metric
\item estimated coefficients
\item overall fit metrics (RMSE, MAE, MSE)
\item compact cross-validation results
}
}
\details{
This method is intended for quick inspection of a model. It is more concise
than \code{summary.smartFit()}, which provides extended diagnostics.

If extra arguments are supplied via \code{...}, they are ignored with a warning
to alert the user.
}
\examples{
set.seed(1)
x <- 1:20; y <- 2 * x + rnorm(20)
fit <- fit_model(x, y, "power_law")
print(fit)
print(fit, foo = "bar") # triggers unused-argument warning

}
